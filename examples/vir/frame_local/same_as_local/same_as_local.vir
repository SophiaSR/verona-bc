class @B
    @f : bool 

class @A
    @f : @B 

func @main() : i32 
    $true = const bool true 
    $b = new @B($true)
    $a = call @make_a($b)

    $ret_val = const i32 42
    ret $ret_val

func @make_a ($b : @B) : @A 
    $a = heap $b @A($b)
    ret $a